By Edward Rodriguez
This project is a checkers game with a blue and read team. It functions completely on typescript and the UI is in HTML. 
This repo in specific cannot deploy. Instead the deployment is done from another repo with the same code. To test the site a local host is recommended.
